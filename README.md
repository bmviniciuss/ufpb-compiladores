# Setup

```bash
$ python3 -m venv env
$ source env/bin/activate
(env) $ pip install .
```

# Run

```bash
(env) $ python main.py
```

# Run a single module

```bash
(env) $ python -m compiler.lexico
```

# Test

```bash
(env) $ pytest
```
